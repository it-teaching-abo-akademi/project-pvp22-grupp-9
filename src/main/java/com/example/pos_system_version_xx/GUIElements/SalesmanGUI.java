package com.example.pos_system_version_xx.GUIElements;

import com.example.pos_system_version_xx.models.PRODUCT_TEST_CLASS;
import javafx.event.EventHandler;
import javafx.fxml.FXML;
import javafx.scene.control.TableColumn;

import javafx.scene.control.cell.PropertyValueFactory;
import javafx.scene.control.cell.TextFieldTableCell;
import javafx.stage.Window;
import javafx.util.converter.DoubleStringConverter;

public class SalesmanGUI extends Window {

    //@FXML private TableView<TODO: Insert Type> salesmanTable;

    //@FXML private TableColumn *insertName*;
    //@FXML private TableColumn *insertName*;
    //@FXML private TableColumn *insertName*;
    //@FXML private TableColumn *insertName*;
    //@FXML private TableColumn *insertName*;

    public SalesmanGUI() {

    }

    /*public void setupSalesmanTable() {
        //CODE TO MAKE TABLES EDITABLE, NEEDED?
        salesmanTable.setEditable(true);
        *insertName*.setCellValueFactory(new PropertyValueFactory<TODO: Insert Table Type, Cell Type>("Cell Name"));
        *insertName*.setCellValueFactory(new PropertyValueFactory<TODO: Insert Table Type, Cell Type>("Cell Name"));
        *insertName*.setCellFactory(TextFieldTableCell.forTableColumn(new TODO: Insert Converter if needed.));
        *insertName*.setOnEditCommit(new EventHandler<TableColumn.CellEditEvent<TODO: Insert Table Type, Cell Type>>() {
            @Override
            public void handle(TableColumn.CellEditEvent<TODO: Insert Table Type, Cell Type> t) {
                TODO: Insert Table Type p = t.getRowValue();
                p.setPrice(t.getNewValue());
            }
        });*/
}
